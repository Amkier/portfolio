# 性能检查与优化（2026-09-23）

## 素材体积

以下为文件字节数，非首屏总下载量；图片按需加载，原始文件保留供原图查看和后续编辑。

| 素材 | 原版 | 当前使用版本 |
| --- | ---: | ---: |
| 54 秒背景视频 | 16,447,209 B | 桌面 8,329,569 B；手机 3,394,433 B |
| 彩色证件照 | 3,867,275 B | WebP 73,272 B |
| 人像、视频封面和 33 张作品页合计 | 12,493,023 B | WebP 4,092,288 B |

视频保持完整时长，分别编码为 1280px / 854px、24fps H.264，移除背景视频无需使用的音轨，使用 faststart。图片另提供 960px 作品页，使用 srcset/sizes、异步解码与懒加载；原图链接仍打开原 JPEG。

## 运行策略

- 背景实际为 HTML video，没有 WebGL、Three.js 或着色器渲染循环。
- 视频封面优先预加载；视频采用 preload=none，在首屏可见且用户允许自动播放时才设置源。
- 深链接直达作品模块时不抢先下载视频。系统减少动态效果或浏览器 Save-Data 模式默认只显示封面，用户仍可主动播放。
- 视频滚出视野、标签页隐藏时暂停；手动暂停状态在滚动往返后保留。
- 导航只保留一个 passive 滚动监听，用 rAF 合并读取 scrollY；模块位置由 ResizeObserver 缓存更新，不逐滚动读取五个布局。
- GSAP 入场执行一次并在卸载时清理；同一标签页仅运行一次 opening，触屏不运行持续人物视差。保留桌面滚动视差及用户要求的揭幕动效。
- 手风琴保留 flex 动画以维持外观，用 layout/style containment 隔离布局，移除长期 will-change。它仍有局部布局开销，不宣称纯 GPU 动画。
- ClickSpark 仅在点击后运行 rAF，粒子结束即停止；页面隐藏时清空，DPR 上限 2。

## 验证

生产构建通过；桌面、手机视频选源正确；离屏暂停、手动暂停保留、减少动态效果不加载视频、作品深链接不加载视频已验证。33 张详情图解码、四个详情入口、复制邮箱、移动菜单及响应式无横向溢出检查通过。

本次没有进行真实手机硬件、公网 CDN 或受控弱网 Lighthouse 对比，因此不提供未经测量的 FPS、LCP 或性能分数。玻璃模糊、遮罩揭幕仍有合成/绘制成本；目前保留其视觉效果，后续可按实际设备数据再调整。
